# M3 implementation working notes — 2026-08-07

- Milestone and acceptance: M3 `In progress`; advances all six automated storage/update/recovery criteria while preserving M0–M2 regression behavior. Human promotion gates remain pending.
- Inspected sources: complete M2/M3 roadmap contracts and gates; root and scoped agent contracts; storage/updater/data/selector/init/xtask sources; kernel configs; CI/release workflows; Cargo/build/source/dependency locks; change, review, security, unsafe, concurrency, architecture, testing, and evidence instructions.
- Invariants: fixed GPT and GUIDs; unchanged `ZEROOSB1`/`ZEROSLT1`; exact signed bytes; monotonic sequence; inactive-slot-only write; flush/reread before pending; one-boot trial; ten healthy seconds after mounted data; independent recovery; data-only explicit reset; bounded v1 additions; no production private key.
- Privilege and untrusted inputs: GPT/sysfs identity, raw partitions, journal/container bytes, HTTPS URLs/headers/bodies, DER keys, clocks, child output/status, API/console input, credentials, terminal state, and external engine failures. All are bounded and validated before mutation.
- Architectures: shared Rust policy targets static-musl x86_64/aarch64; fallback EFI names and selector ABI remain target-specific. ARM64 weak ordering is unaffected; PID 1 signal flags remain notification-only atomics.
- Rollback/interruption: partial downloads require durable matching metadata; slot writes do not stage until flush/reread/reverify; journal alternates records; unconfirmed trials roll back; recovery/reset never target system partitions; child failure leaves boot state unchanged.
- Required tests: format; warnings-denied Clippy; workspace tests; dependency/advisory policy; strict parser/signature/redirect/resume/GPT/journal/API/terminal/redaction tests; pinned-image `cargo xtask check`; both `cargo xtask test --arch` commands; disposable Secure Boot/update/data fault matrix; real human gates only at promotion.
- Deliberate scope: no new on-disk/API version and no M4+ implementation. Production ceremony, protected signer provisioning, independent review, release publication, and achievement tag cannot be fabricated locally.
- Verification run 2026-08-07: pinned `cargo fmt --all -- --check`, warnings-denied workspace Clippy, workspace tests, `cargo deny check`, and `cargo xtask check` passed. Native aarch64 `cargo xtask test --arch aarch64` passed after fixing data-engine lookup to use the initramfs's exact `/usr/sbin` paths; the final passing development disk hash was `8875f1a1afcb4a563a67b03aeef7629c38f5a9ff08d0e9fd9427a25ac60fc173` and is not release evidence. Native x86_64 remains for its x86_64 CI runner and must not be inferred from ARM.
- Acceptance still outstanding: local-HTTPS update installation, interruption/fault matrix, credential marker preservation, recovery repair/reset QEMU scenarios, x86_64 Secure Boot/rotation execution, and every production/human gate. M3 therefore remains `In progress`.
- 2026-08-10 continuation: reused the production journal and manifest parsers plus a shared 12-byte container-header decoder for a pinned, fixed-seed bounded fuzz smoke target. Acceptance-only init/updater builds now expose bounded download, verification, slot-write/flush/reread, journal, reboot, and health markers; production artifact inspection rejects those hooks. Release dispatch now requires a full source SHA with a successful exact-SHA CI run, reviewed GitHub environments, the release token, native protected production runners, and a version/hash-pinned `zeroos-sign`; provenance must contain public fingerprints and no private/secret material labels. The pinned-image `cargo xtask check` passed locally on native aarch64, including 1000 fuzz executions; this is development verification, not milestone evidence.
- 2026-08-10 Secure Boot continuation: `cargo xtask test --arch aarch64` passed locally in the pinned image with isolated disposable-key QEMU cases for valid boot and unsigned/altered selector, normal-slot, and recovery rejection. It also passed current/next db overlap, next-key boot after old-db removal, and old-key rejection. The runner retained redacted scenario logs and before/after hashes for all six partitions and each UEFI variable store. x86_64 native evidence, local HTTPS/update interruption, recovery/reset, and all external promotion gates remain outstanding; M3 stays `In progress`.
- 2026-08-11 continuation: disposable Secure Boot private keys now live only in a process-specific temporary directory with deterministic cleanup, and production inspection streams across init, updater, data tool, selector, kernel, raw slots, and the complete disk for acceptance origins, hooks, test identities, certificates, and private-key markers. The release workflow now checks exact-main successful CI, next public sequence, protected environment configuration, architecture-specific signer hashes, committed public trust, and exact provenance fingerprints; it publishes a prerelease, runs unauthenticated native `test-release` installation/normal/recovery Secure Boot, and promotes only after both jobs pass. `cargo xtask check` passed in the pinned aarch64 image after these changes, including formatting, warnings-denied Clippy, workspace tests, dependency policy, 1000 fixed-seed fuzz executions, and reproducibility. The native aarch64 runtime/Secure Boot matrix also passed before the final release-only jq/trust parsing edits; those edits do not change guest behavior, but this local run is not milestone evidence.
- 2026-08-11 external-gate audit: the private source repository has no configured environments, no self-hosted runners, and no collaborator other than Kordian; the public release repository has no releases. No independent reviewer, ceremony evidence, signer audit, backup/restore evidence, public trust files, or production artifacts are available. The HTTPS update/interruption and recovery/repair/reset QEMU matrices also remain unwired. M3 stays `In progress`; no evidence commit or `m3-achieved` tag is authorized.
- 2026-08-11 architecture-boundary correction: production `x86_64` and `aarch64` kernel fragments now explicitly disable kernel IP autoconfiguration; DHCP and `ip=dhcp` exist only in target-specific acceptance fragments. The pinned native aarch64 `cargo xtask test --arch aarch64` runtime, data provisioning, Secure Boot rejection, recovery fallback, and disposable rotation suite passed after this correction with development disk hash `e2ae3e340ec5019b723640600da2878054101bbddeb312cf6b994cec3cd89924`. This remains local development verification; native x86_64 and the unwired matrices remain outstanding.
- 2026-08-12 protected-signer continuation: accepted ADR 0001 selects YubiHSM 2 through PKCS#11 for non-exporting RSA-3072 Secure Boot and RSA-PSS/SHA-256 release signing. The minimal `zeroos-sign` implements the workflow's five interfaces, reads only a root-owned fixed configuration path in production, rejects authentication-bearing PKCS#11 URIs, suppresses engine output, and atomically installs package/provenance files. Native CI builds and hashes it separately. Pinned `cargo xtask check` passed with workspace format/Clippy/tests, policy/license review, 1000 fuzz executions, reproducible signer builds, and an actual disposable SoftHSM RSA-3072/PSS sign-and-verify path. The YubiHSM module is source/checksum pinned for protected-runner provisioning; hardware provisioning and ceremony remain external gates.
- 2026-08-12 update-acceptance continuation: target-specific acceptance kernels now add only DHCP and virtio-net to production fragments. `cargo xtask test --arch` builds separate acceptance-only init/updater/kernel artifacts with an embedded disposable CA/origin and release public key, serves a strong-ETag fixed slot over local HTTPS, installs it through the guest's real updater, checks every persisted phase marker through atomic journal switch/reboot, restarts the same disk, and requires sequence 1 to become health-confirmed. Production scanning still rejects every acceptance identity, origin, key, and hook. The current aarch64 execution result and the remaining fault/recovery matrix must be recorded after they complete; M3 remains `In progress`.
- 2026-08-12 acceptance result: the pinned native aarch64 recovery/update matrix passed after correcting block-device capacity checks to use the existing sysfs/GPT identity boundary, giving recovery children exclusive terminal ownership, making `repair-data` unattended, and exposing acceptance hooks directly on the console. It covered both-slot recovery, `repair-boot`, `repair-data`, rejected reset, confirmed factory reset with new credentials, immutable system/recovery hashes, six persisted update cut points with same-image restart, health confirmation, and reboot-before-confirmation rollback. This is local development verification; native x86_64 and every production/human gate remain pending, so M3 stays `In progress`.
- 2026-08-13 single-operator promotion working note: M3 remains `In progress`; this change advances only its key and promotion gate by recording ADR 0002 and enforcing owner-only, exact-main/exact-SHA promotion. Inspected the complete M3 roadmap contract and ledger rules, ADR 0001, release/check workflows, M3 threat/test plans, public-trust validation, and change/review/evidence protocols. The fixed GPT, `ZEROOSB1`, `ZEROSLT1`, `ZEROOS/1`, key algorithms, non-exporting signer, secret exclusion, inactive-slot durability, rollback, independent recovery, and both-architecture acceptance requirements remain unchanged. GitHub inputs, release artifacts, HSM output, runner state, and offline ceremony records are untrusted until validated; authentication material must remain outside Git, arguments, ordinary environment values, artifacts, and logs. Both native architectures require exact-SHA CI and public install evidence. Failure or interruption before promotion leaves the release a prerelease and M3 unachieved; suspected operator or runner compromise disables both environments/runners and revokes or replaces affected keys. Required verification before implementation: workflow syntax/policy checks, format, warnings-denied Clippy, workspace tests, dependency policy, `cargo xtask check`, host acceptance, and both native architecture suites; external ceremony, backup/restore, HSM audit, release, CI, and tag gates must be recorded only when actually performed.
- 2026-08-13 verification result: the pinned Rust 1.97.1 image passed `cargo fmt --all -- --check`, warnings-denied workspace Clippy, workspace tests, `cargo deny check`, `cargo xtask check`, `cargo xtask test`, and native `cargo xtask test --arch aarch64`; release workflow YAML parsing and `git diff --check` also passed. Host Cargo was not used because its Rust 1.94.0 is below the workspace requirement. Native x86_64, exact-candidate CI, public trust material, environments/runners, HSM ceremony/audit, backup/restore, recovery signing, immutable public release, evidence commit, and tag remain unperformed external gates. M3 remains `In progress` with all six acceptance boxes unchecked.
- 2026-08-13 GitHub provisioning: created `offline-recovery-draft` and `production-release` with custom deployment policy restricted to the single `main` branch and no reviewer rule, and enabled immutable releases on `KZagaja/zeroOS-releases`. API rereads confirmed both exact branch policies and `enabled: true`. The release token, signer variables, native runners, and all ceremony/release evidence remain absent, so these provisioning steps do not achieve M3.
- 2026-08-13 runner preparation: the authenticated repository runner-download API selected Actions Runner 2.336.0. Downloaded the exact Linux packages from the returned URLs: x86_64 `actions-runner-linux-x64-2.336.0.tar.gz` SHA-256 `04cf0be1aff4c3ec3554466c39124ca250e3effd8873bb7e8d68535aa9505d5d`; aarch64 `actions-runner-linux-arm64-2.336.0.tar.gz` SHA-256 `58b758e420b87093fbd4bfddd368074960053e2f1388f01848c82624b90f27d1`. No runner hosts are reachable or registered yet, so installation and service hardening remain pending.
- 2026-08-14 candidate CI: commit `b05974b41dbfbbc188347b9649d156a6127f9fa1` passed both native jobs in run 31744097927. Downloaded and independently verified its signer artifacts: x86_64 SHA-256 `9945e5f2ea6a90bdebd9c7564480b0b6ccb2c66d80a5ab18f6dcfada0ecac0b7`; aarch64 SHA-256 `df6184eb5a0e944a0f281115134b0493f33a60fd652332a03f438309e3ebb6aa`. Configured those digests and `zeroos-sign 0.1.0` in `production-release`. The supplied SSH host is Fedora 44 x86_64 with no passwordless sudo, Docker, attached YubiHSM, or dedicated-workload isolation, so it was inspected read-only and not registered under the accepted Ubuntu 24.04 production-runner contract.
- 2026-08-14 single-signer redesign working note: M3 remains `In progress`; ADR 0003 will replace only ADR 0002's dual self-hosted signer requirement. GitHub-hosted native x86_64 and aarch64 jobs will build and execute acceptance, then an isolated self-hosted x86_64 Linux host will verify the exact-run unsigned artifacts and sign/package both architectures through the non-exporting HSM. The unsigned artifact boundary, GitHub Actions artifact transport, recovery hashes, source SHA, sequence, public trust, signer binary, and signing host are untrusted inputs requiring exact validation. All key custody, secret exclusion, algorithms, formats, native acceptance, public install, rollback, offline ceremony, backup/restore, and revocation gates remain unchanged. Failure before final publication leaves only an immutable candidate prerelease; contain signing-host compromise by disabling the environment/runner and revoking or replacing affected keys. Required checks: workflow syntax, format, warnings-denied Clippy, workspace tests, dependency policy, host acceptance, and exact-SHA native CI before production dispatch.
- 2026-08-14 single-signer local verification: release workflow YAML parsing, `git diff --check`, host `cargo fmt --all -- --check`, and pinned-image `cargo xtask check` passed, including warnings-denied Clippy, workspace tests, dependency policy, SoftHSM signing, fuzz smoke, and reproducibility. Exact-SHA native CI remains required after the ADR 0003 commit is pushed.
- 2026-08-14 hardware-deferral decision: accepted ADR 0004 because YubiHSM hardware is unavailable before M3. M3 keeps the existing PKCS#11 signer interface and all algorithms, signed-update, Secure Boot, recovery, backup/restore, revocation, immutable-release, secret-exclusion, exact-SHA, and native dual-architecture gates, but permits a software token on encrypted storage and hash-chained host signing logs with explicit signing-host extraction risk. M11 must generate new YubiHSM-resident keys, migrate trust, revoke the software keys, and complete HSM audit/backup/restore evidence before any 1.0 release. M3 remains `In progress` until its amended gates actually pass.
- 2026-08-14 hardware-deferral verification: pinned-image `cargo xtask check` passed, including formatting, warnings-denied Clippy, workspace tests, dependency/source policy after removing the premature YubiHSM pin, disposable SoftHSM signing, parser fuzz smoke, and reproducible release-tool builds. Workflow YAML parsing and complete-diff checks also passed. Exact-SHA native CI and the amended external promotion gates remain required.
