# Dependency admission: <name> <version>

- Requirement / milestone:
- Owner:
- Standard library, kernel/native, and existing-dependency alternatives:
- Why those are insufficient:
- Maintenance status and vulnerability response:
- License and distribution/notice/source obligations:
- Exact version pin, source URL, checksum, and reproducible acquisition:
- x86_64 support / evidence:
- aarch64 support / evidence:
- Direct and transitive dependencies reviewed:
- Runtime/build/base-image presence:
- Privileges, inputs, attack surface, and isolation:
- Update path:
- Classification: `Retain` or `Replace`
- Replacement trigger, exit criteria, and adapter boundary (`Replace` only):
- Review date and approver:

Add the machine-readable ledger entry and run `cargo xtask check`.
