# ADR: <decision>

- Status: Proposed
- Date / owner:
- Roadmap milestone and acceptance item:
- Supersedes / conflicts:

## Context and invariants

State the concrete decision gap, existing repository evidence, trust/privilege boundaries, durable/API compatibility, both-architecture constraints, rollback, and recovery requirements.

## Options

For each option record security and failure model, operational complexity, dependencies/licenses, performance evidence, migration/exit path, and x86_64/aarch64 support. Include “do nothing”.

## Decision and consequences

Record the selected option, why alternatives fail current requirements, validation commands, rollout/rollback, and conditions that reopen the decision. Acceptance changes require an appended `ROADMAP.md` change-log entry; never rewrite history.
